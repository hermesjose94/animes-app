import React from "react";

import "../assets/styles/components/GridCards.scss";

const GirdCards = ({ children }) => (
  <div className="content-wrapper">{children}</div>
);

export default GirdCards;
